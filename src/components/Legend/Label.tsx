import * as React from 'react';

interface Props {
    color: string;
    name: string;
    start: Date;
    stop: Date;
}

export default ({ color, name, start, stop }: Props) => (
    <div></div>
);
