import * as React from 'react';

export default class App extends React.Component<undefined, undefined> {
    render() {
        return (
            <div>
                Hello world
            </div>
        );
    }
}
